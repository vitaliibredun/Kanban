import constants.Status;
import service.TaskManager;
import task.Epic;
import task.SubTask;
import task.Task;

public class Main {

    public static void main(String[] args) {


        
        /*TaskManager taskManager = new TaskManager();

        Task task1 = new Task("Task 1", Status.NEW, "the 1 task");
        Task task2 = new Task("Task 2", Status.IN_PROGRESS, "the 2 task");

        taskManager.addTask(task1);
        taskManager.addTask(task2);


        Epic epic1 = new Epic("Epic 1", Status.NEW, "the 1 epic");
        Epic epic2 = new Epic("Epic 2", Status.DONE, "the 2 epic");

        taskManager.addEpic(epic1);
        taskManager.addEpic(epic2);

        SubTask subTask1 = new SubTask("Subtask 1", Status.IN_PROGRESS, "the subtask 1", epic1.getId());
        SubTask subTask2 = new SubTask("Subtask 2", Status.NEW, "the subtask 2", epic2.getId());

        taskManager.addSubtask(subTask1);
        taskManager.addSubtask(subTask2);*/

    }
}
